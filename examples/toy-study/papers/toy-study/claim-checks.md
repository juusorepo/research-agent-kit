# Literature claim checks

One row per statement that was checked against a source in this paper. Not an audit report. Not an approved claim. A later **Audit literature claims** still reads the source.

The `check:` field on an AI-use event is the whole run. This table is the per-statement record.

Do not load this file by default. Add a row when you actually open a source and check a statement.

| date | citation key | manuscript location | source file | what was verified |
|---|---|---|---|---|
|  |  |  |  |  |
