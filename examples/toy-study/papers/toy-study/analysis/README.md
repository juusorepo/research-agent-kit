# R analysis stub

Default analysis language in v0.1. Put scripts in the `scripts` path from `layout.yml`.

Write **draft** outputs with output metadata. Do not mark them approved. If `data_access` is `restricted`, develop against synthetic or development data only.
