# Sources (optional copies)

PDF copies for sharing and NotebookLM live here (`paths.sources` in `layout.yml`). Canonical identity is the Zotero item (`zotero:<item-key>`). The manuscript bibliography is still `references.bib`. Restricted files stay out.
